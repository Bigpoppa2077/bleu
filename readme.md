# BLEU

A lightweight creative project assistant (draft plan, constraints, pipeline health, change log, vision integrity, progress report).

## Requirements
- Node.js 18+ (recommended)

## Run locally
```bash
git clone <this-repo-url>
cd bleu/web
npm install
npm run dev
